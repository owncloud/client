// model-body.mustache

// licenseInfo.mustache

/**
 * Libre Graph API
 * Libre Graph is a free API for cloud collaboration inspired by the MS Graph API.
 *
 * The version of the OpenAPI document: v1.0.4
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OAISharePointIdentitySet.h"

#include <QDebug>
#include <QJsonArray>
#include <QJsonDocument>
#include <QObject>

#include "OAIHelpers.h"

namespace OpenAPI {

class OAISharePointIdentitySetPrivate {
    friend class OAISharePointIdentitySet;

     OAIIdentity user;
     bool user_isSet;
     bool user_isValid;

     OAIIdentity group;
     bool group_isSet;
     bool group_isValid;
};

OAISharePointIdentitySet::OAISharePointIdentitySet()
    : d_ptr()
{
}

OAISharePointIdentitySet::OAISharePointIdentitySet(const OAISharePointIdentitySet& other)
    : d_ptr(other.d_ptr)
{
}

OAISharePointIdentitySet::OAISharePointIdentitySet(QString json)
    : d_ptr(nullptr)
{
    this->fromJson(json);
}

OAISharePointIdentitySet::~OAISharePointIdentitySet() = default;

void OAISharePointIdentitySet::initializeModel() {
    if (d_ptr == nullptr) {
        d_ptr.reset(new OAISharePointIdentitySetPrivate{});

        Q_D(OAISharePointIdentitySet);


        d->user_isSet = false;
        d->user_isValid = false;

        d->group_isSet = false;
        d->group_isValid = false;
    }
}

void OAISharePointIdentitySet::fromJson(QString jsonString) {
    QByteArray array(jsonString.toUtf8());
    QJsonDocument doc = QJsonDocument::fromJson(array);
    QJsonObject jsonObject = doc.object();
    this->fromJsonObject(jsonObject);
}

void OAISharePointIdentitySet::fromJsonObject(QJsonObject json) {
    initializeModel();

    Q_D(OAISharePointIdentitySet);

    d->user_isValid = ::OpenAPI::fromJsonValue(d->user, json[QString("user")]);
    d->user_isSet = !json[QString("user")].isNull() && d->user_isValid;

    d->group_isValid = ::OpenAPI::fromJsonValue(d->group, json[QString("group")]);
    d->group_isSet = !json[QString("group")].isNull() && d->group_isValid;
}

QString OAISharePointIdentitySet::asJson() const {
    QJsonObject obj = this->asJsonObject();
    QJsonDocument doc(obj);
    QByteArray bytes = doc.toJson();
    return QString(bytes);
}

QJsonObject OAISharePointIdentitySet::asJsonObject() const {
    Q_D(const OAISharePointIdentitySet);
    if(!d){
        return {};
    }
    QJsonObject obj;
    if (d->user.isSet()) {
        obj.insert(QString("user"), ::OpenAPI::toJsonValue(d->user));
    }
    if (d->group.isSet()) {
        obj.insert(QString("group"), ::OpenAPI::toJsonValue(d->group));
    }
    return obj;
}

OAIIdentity OAISharePointIdentitySet::getUser() const {
    Q_D(const OAISharePointIdentitySet);
    if(!d){
        return {};
    }
    return d->user;
}
void OAISharePointIdentitySet::setUser(const OAIIdentity &user) {
    Q_D(OAISharePointIdentitySet);
    Q_ASSERT(d);

    d->user = user;
    d->user_isSet = true;
}

bool OAISharePointIdentitySet::is_user_Set() const{
    Q_D(const OAISharePointIdentitySet);
    if(!d){
        return false;
    }

    return d->user_isSet;
}

bool OAISharePointIdentitySet::is_user_Valid() const{
    Q_D(const OAISharePointIdentitySet);
    if(!d){
        return false;
    }
    return d->user_isValid;
}

OAIIdentity OAISharePointIdentitySet::getGroup() const {
    Q_D(const OAISharePointIdentitySet);
    if(!d){
        return {};
    }
    return d->group;
}
void OAISharePointIdentitySet::setGroup(const OAIIdentity &group) {
    Q_D(OAISharePointIdentitySet);
    Q_ASSERT(d);

    d->group = group;
    d->group_isSet = true;
}

bool OAISharePointIdentitySet::is_group_Set() const{
    Q_D(const OAISharePointIdentitySet);
    if(!d){
        return false;
    }

    return d->group_isSet;
}

bool OAISharePointIdentitySet::is_group_Valid() const{
    Q_D(const OAISharePointIdentitySet);
    if(!d){
        return false;
    }
    return d->group_isValid;
}

bool OAISharePointIdentitySet::isSet() const {
    Q_D(const OAISharePointIdentitySet);
    if(!d){
        return false;
    }
    bool isObjectUpdated = false;
    do {
        if (d->user.isSet()) {
            isObjectUpdated = true;
            break;
        }

        if (d->group.isSet()) {
            isObjectUpdated = true;
            break;
        }
    } while (false);
    return isObjectUpdated;
}

bool OAISharePointIdentitySet::isValid() const {
    Q_D(const OAISharePointIdentitySet);
    if(!d){
        return false;
    }
    // only required properties are required for the object to be considered valid
    return true;
}

} // namespace OpenAPI
