Change: We ended support for OWNCLOUD_FORCE_DARK_MODE environment variable

The application now relies exclusively on the chosen system theme to determine if dark mode should be used.

https://github.com/owncloud/client/pull/12625